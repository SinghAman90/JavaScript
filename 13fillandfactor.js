var testArray = [2,4,6,3,1,5,9,8];

console.log(testArray.fill('h', 2, 5)); //what to fill, position fron where to fill, end position

const myNumbers = [21,23,45,65,47,32,98,76]
const result = myNumbers.filter( (num) => num > 40); //filter function
console.log(result);